specVersion: "v1"
serviceId: "rich"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "rich-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
